/*
 * Footer Skip Link Component
 *
 * Reach UI 'skip-nav' component: https://reacttraining.com/reach-ui/skip-nav
 */

import React from "react"
import { SkipNavLink } from "@reach/skip-nav"

const SkipLinkFooter = () => {
    return (
        <>
            <SkipNavLink>Skip footer, go back to main content</SkipNavLink>
        </>
    )
}

export default SkipLinkFooter
